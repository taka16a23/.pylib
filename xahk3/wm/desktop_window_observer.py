#!/usr/bin/env python
# -*- coding: utf-8 -*-
r"""desktop_window_observer -- DESCRIPTION

"""


class DesktopWindowObserver(object):
    r"""DesktopWindowObserver

    DesktopWindowObserver is a object.
    Responsibility:
    """
    def on_window_bounds_changed(self, window):
        r"""SUMMARY

        on_window_bounds_changed(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_title_changed(self, window):
        r"""SUMMARY

        on_window_title_changed(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_pid_changed(self, window):
        r"""SUMMARY

        on_window_pid_changed(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_fullscreened(self, window):
        r"""SUMMARY

        on_window_fullscreened(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_unset_fullscreened(self, window):
        r"""SUMMARY

        on_window_unset_fullscreened(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_maximized(self, window):
        r"""SUMMARY

        on_window_maximized(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_unset_maximized(self, window):
        r"""SUMMARY

        on_window_unset_maximized(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_minimized(self, window):
        r"""SUMMARY

        on_window_minimized(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_unset_minimized(self, window):
        r"""SUMMARY

        on_window_unset_minimized(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_shaded(self, window):
        r"""SUMMARY

        on_window_shaded(window)

        @Arguments:
        - `window`:

        @Return:

        @Error:
        """

    def on_window_destroyed(self, window_id):
        r"""SUMMARY

        on_window_destroyed(window_id)

        @Arguments:
        - `window_id`:

        @Return:

        @Error:
        """



# For Emacs
# Local Variables:
# coding: utf-8
# End:
# desktop_window_observer.py ends here
